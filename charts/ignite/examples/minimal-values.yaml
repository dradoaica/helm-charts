# Minimal values for Apache Ignite 2
# Use this for development, testing, or resource-constrained environments
#
# Install with:
#   helm install ignite ./charts/ignite -f ./charts/ignite/examples/minimal-values.yaml

replicaCount: 1
